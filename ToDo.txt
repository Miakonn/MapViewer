// Buggar

System.ArgumentException: "infinity" är inte ett giltigt värde för egenskapen Width.
   vid System.Windows.DependencyObject.SetValueCommon(DependencyProperty dp, Object value, PropertyMetadata metadata, Boolean coerceWithDeferredReference, Boolean coerceWithCurrentValue, OperationType operationType, Boolean isInternal)
   vid System.Windows.DependencyObject.SetValue(DependencyProperty dp, Object value)
   vid MapViewer.Symbols.SymbolRectangle.Draw(CanvasOverlay canvas, MapDrawSettings settings)

// 	Nya funktioner

Hög prio ================================

* mer ångra-funktion på symboler

* lägg till fler status, "Fire", "RedCross", "Cross", "GreenCross"

Medel prio =================================

* Klipp och klistra, mellan levels

* Markera alla med Ctrl-a

* Spara delmängd av symboler

* symboler utan att kalibrera bild?

* kolla indata i dialogprop

* gummiband vid förflyttning

* status på pc

* bild på pc eller pc-flagga på bild

Låg prio ------------------------

* släppa låsta bildfiler?

* rotera alla objekt?

* riktning på creature?

* ändra text-storlek

* gruppera symboler

* visa rutan före publicering

* fixa så zoom är runt cursor

* tillåt rotering av kompass

* hit points i prop dialogen?